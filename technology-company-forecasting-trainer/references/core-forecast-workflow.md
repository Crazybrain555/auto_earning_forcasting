# Core Forecast Workflow

The canonical workflow is `references/research-sop.md`; the machine-readable
stage registry is `assets/method_system.json`.  This file is retained as a
compatibility route for older prompts.  It must not maintain another numbered
process.

Load those two canonical files and follow them directly. This compatibility
route defines no stage, gate, artifact or profile semantics of its own.
